#include <stdio.h>

int main() {

    const int SECONDS_PER_MINUTE = 60;
    const int MINUTES_PER_HOUR = 60;
    const float PI = 3.14;
    printf("Segundos: %d\n", SECONDS_PER_MINUTE);
    printf("Minutos: %d\n", MINUTES_PER_HOUR);
    printf("PI: %.2f\n", PI);

    return 0; 
}